/// <reference types="@webgpu/types" />
// Pulls the WebGPU global type definitions (GPUDevice, GPUCanvasContext, …) into
// the project without restricting the default `@types/*` inclusion in tsconfig.
