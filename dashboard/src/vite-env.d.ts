/// <reference types="vite/client" />

// Injected at build time by vite.config.ts `define` — the resolved API base URL
// (VITE_API_URL in prod, http://localhost:4000 fallback in dev).
declare const __LITEGEN_API_BASE__: string;
